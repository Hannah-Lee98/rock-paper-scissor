import React from 'react';

const UnMuteWhiteIcon = () => {
  return (
    <img
      alt={''}
      src={'/images/volume-white.png'}
      style={{
        width: 25,
        height: 'auto',
        cursor: 'pointer',
      }}
    />
  );
};

export default UnMuteWhiteIcon;
