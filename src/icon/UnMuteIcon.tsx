import React from 'react';

const UnMuteIcon = () => {
  return (
    <img
      alt={''}
      src={'/images/volume.png'}
      style={{
        width: 25,
        height: 'auto',
        cursor: 'pointer',
      }}
    />
  );
};

export default UnMuteIcon;
